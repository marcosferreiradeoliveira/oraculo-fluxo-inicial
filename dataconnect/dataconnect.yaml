specVersion: "v1"
serviceId: "oraculo-fluxo-inicial"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "oraculo-fluxo-inicial-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
