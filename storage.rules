rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Regra temporária para teste - permitir tudo para usuários autenticados
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
